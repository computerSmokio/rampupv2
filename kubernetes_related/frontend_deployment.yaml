apiVersion: v1
kind: Deployment
metadata:
  labels:
    app: frontend-movie
  name: frontend-movie
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fontend_movie
  template:
    metadata:
      labels:
        app: frontend-movie
    spec:
      containers:
      - image: 419466290453.dkr.ecr.sa-east-1.amazonaws.com/rampup-frontend:latest
        imagePullPolicy: Always
        name: frontend-movie
---
apiVersion: v1
kind: Service
metadata:
    labels:
        app: frontend-movie
    name: frontend-svc
spec:
    ports:
    - port: 3030
      protocol: TCP
      targetPort: 3030
    selector:
        app: frontend-movie
    type: NodePort